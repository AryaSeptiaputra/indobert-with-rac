# IndoBERT-with-RAC — dependensi Python
# Instalasi: pip install -r requirements.txt
#
# Catatan GPU: faiss-cpu adalah default untuk kerja lokal (EDA/preprocessing).
# Untuk fase training di Vast.ai (GPU), ganti ke: faiss-gpu==1.7.2
# dan gunakan image pytorch/pytorch:2.1.0-cuda12.1-cudnn8-runtime.

torch==2.1.0
transformers==4.38.0
datasets==2.18.0
sentencepiece              # transformers butuh ini utk *mengenali* kelas AlbertTokenizer,
                           # walau load_tokenizer() akhirnya fallback ke BertTokenizer utk
                           # indobert-lite-* (repo Hub-nya cuma punya vocab.txt WordPiece,
                           # bukan .model SentencePiece asli -- lihat src/dataset.py)
faiss-cpu==1.7.4          # ganti faiss-gpu==1.7.2 bila ada GPU (Vast.ai)
scikit-learn==1.4.0
pandas==2.2.0
numpy==1.26.4
openpyxl==3.1.2
matplotlib==3.8.0
seaborn==0.13.2
tqdm==4.66.0
langdetect==1.0.9         # deteksi bahasa (EDA Bagian 1)

# Tuning Control Panel (app.py) — dijalankan di instance Vast.ai
streamlit>=1.30

# Menjalankan notebook (.ipynb) di lingkungan headless
nbclient==0.11.0
nbformat==5.10.4
ipykernel
