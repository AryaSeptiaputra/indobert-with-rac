# IndoBERT-with-RAC — dependensi khusus instance Vast.ai (GPU)
#
# Dipakai untuk menjalankan Tuning Control Panel (app.py) + worker src/job_runner.py.
# Instalasi di instance:
#     pip install -r requirements-vast.txt
#
# ---------------------------------------------------------------------------
# ⚠️  torch SENGAJA TIDAK DICANTUMKAN — dan jangan ditambahkan.
#
# Instance memakai template **PyTorch** Vast.ai (CUDA 12.x, varian `runtime`),
# jadi torch sudah terpasang dan sudah cocok dengan driver CUDA-nya.
# Mencantumkan torch di sini berisiko pip MENURUNKAN versi bawaan template.
#
# Syarat versi: **torch >= 2.4**. Kode memakai API `torch.amp` dengan argumen
# device — `torch.amp.GradScaler("cuda", ...)` di src/tuning.py dan
# `torch.amp.autocast("cuda", ...)` di src/job_runner.py — yang baru ada sejak 2.4.
# Di bawah itu training akan gagal.
#
# Verifikasi setelah instance menyala:
#     python -c "import torch; print(torch.__version__, torch.cuda.is_available()); torch.amp.GradScaler('cuda')"
#
# ⚠️  JANGAN jalankan `pip install -r requirements.txt` di instance.
# File itu untuk lingkungan LOKAL (EDA/preprocessing) dan memaku torch==2.1.0,
# yang akan merusak `torch.amp`.
# ---------------------------------------------------------------------------

# UI control panel
streamlit>=1.30

# Model & tokenizer IndoBERT (indobenchmark/indobert-base-p2)
transformers>=4.38

# Retrieval RM-c. faiss-cpu sudah lebih dari cukup: index hanya ~6.588 vektor
# 768-dim dari train set. faiss-gpu tidak sepadan dengan risiko konflik CUDA-nya.
faiss-cpu

# Metrik klasifikasi + split/class-weight (src/evaluate.py, src/preprocessing.py)
scikit-learn

# I/O riwayat run (runs_*.csv), tabel UI
pandas

# Operasi embedding & fusi logit (src/rac.py, src/modeling.py)
numpy

# Confusion matrix -> results/vast/figures/ (dipakai headless, MPLBACKEND=Agg)
matplotlib

# ---------------------------------------------------------------------------
# TIDAK diperlukan di instance (sengaja dihilangkan dari requirements.txt lokal):
#   seaborn     - tidak dipakai di src/ maupun app.py
#   langdetect  - hanya untuk notebook EDA Bagian 1, dijalankan lokal
#   datasets    - tidak di-import di jalur training mana pun
#   openpyxl    - baca Excel saat pelabelan, lokal
#   tqdm        - tidak di-import oleh app.py/src/ pada jalur Vast.ai
#   nbclient / nbformat / ipykernel - eksekusi notebook, lokal
# ---------------------------------------------------------------------------
