# IndoBERT-with-RAC - dependensi produksi
# Instalasi: pip install -r requirements.txt
#
# Torch di-pin tanpa suffix build. Untuk GPU, pasang lebih dulu dari index CUDA
# yang sesuai, mis. CUDA 13.0:
#   pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cu130
# Versi di bawah adalah yang benar-benar terpasang dan tervalidasi di Python 3.13.

torch==2.12.1
transformers==5.12.1
sentencepiece==0.2.2       # dibutuhkan transformers untuk mengenali kelas AlbertTokenizer
                           # sebelum fallback WordPiece berjalan (lihat comment_dataset.py)

faiss-cpu==1.14.3          # retrieval RM-c; indeks flat/exact, CPU sudah memadai
scikit-learn==1.8.0
pandas==2.3.3
numpy==2.4.0

pydantic==2.13.5
pydantic-settings==2.15.0
python-json-logger==4.2.0

matplotlib==3.10.8
seaborn==0.13.2
openpyxl==3.1.5
tqdm==4.68.3
psutil==7.2.2              # pengukuran memori proses pada benchmark FAISS
langdetect==1.0.9          # deteksi bahasa (EDA)

streamlit==1.59.2          # control panel app.py
