# Python
__pycache__/
*.py[cod]
build/
dist/
*.egg-info/

# Virtual environment
venv/
.venv/
env/

# Jupyter
.ipynb_checkpoints/

# Dataset mentah & hasil preprocessing
# alasan: regenerable dari raw + script preprocessing, dan berisi data personal (username YouTube)
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# Checkpoint model & index FAISS — besar, hasil training di Vast.ai
checkpoints/
*.bin
*.safetensors
*.pt
*.faiss
*.index

# Log training
runs/
logs/
wandb/

# Hasil tuning Vast.ai — regenerable dari re-run kampanye, backup lewat report_bundle.zip
# (tab Hasil app.py); menumpuk terus tiap sesi, tak perlu ikut versi kontrol.
results/vast/
# out_dir eksperimen ad-hoc (mis. results/vast_rmc_cheap_head/, results/vast_candidate2/)
# -- sama alasannya: regenerable, biner besar, bukan hasil resmi Bab 4.
results/vast_*/

# Cache embedding beku RM-b/RM-c — regenerable dari model+data (28MB biner, sama alasan
# dengan checkpoints/*.pt di atas).
results/features/

# Kredensial (kalau nanti nyimpen API key Vast.ai / GitHub token)
.env
*.pem

# OS / editor
.DS_Store
.vscode/
.idea/

# Claude Stuff
./claude
CLAUDE.MD